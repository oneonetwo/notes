## 第三章∶ 3-linux操作系统使用
一. [操作系统介绍](#一-操作系统介绍)  
二. [Linux目录基本操作](#二-Linux目录基本操作)  
三. [Linux目录文件操作选项-批量修改文件名](#三-Linux目录文件操作选项-批量修改文件名)  
四. [Linux远程连接及软件操作](#四-Linux远程连接及软件操作)  
五. [linux命令补充](#五-linux命令补充)  
<!-- 六. [面向对象案例-烤地瓜](#六-面向对象案例-烤地瓜)  
七. [案例-搬家具](#七-案例-搬家具)  
八. [面向对象-单继承](#八-面向对象-单继承)  
九. [面向对象-多继承及私有属性](#九-面向对象-多继承及私有属性)  
十. [面向对象-类属性、静态方法、多态](#十-面向对象-类属性、静态方法、多态)  
十一. [面向对象试题讲解及作用讲解](#十一-面向对象试题讲解及作用讲解)  
十二. [Python错误处理-Exception](#十二-Python错误处理-Exception)  
十三. [Python模块与包](#十三-Python模块与包)   -->


### 一. 操作系统介绍
1. 操作系统：直接运行在计算机上的系统软件，他是控制硬件和支持软件运行的计算机程序。 
    1. 作用：承上启下，向上支持其他应用软件的运行，向下控制硬件
    2. 常见类型：Window, Mac OS, linux, Android
2. 虚拟机软件
    1. 虚拟计算机的硬件，模拟真实的计算机，便于安装操作系统
3. ubuntu操作系统
    1. 没有盘符的概念，直接就是根目录
4. Linux内核和发行版
    1. 内核：操作系统内部操作和控制硬件设备的核心程序，他是芬兰人 林纳斯开发的
    2. 常见的发行版操作系统： Ubuntu, Centos, Redhat 
5. widow和linux的区别
6. 计算机的存储单位
### 二. Linux目录基本操作
1. 查看目录的命令 `ls`,查看当前目录的信息。`tree`,树状的形式查看当前目录， 终端命令格式的组成：`命令 [-选项] [参数]` 选项可以放在一起书写
    1. `ls` 选型`-l详细信息 -h文件大小 -a全部文件` 
        1. `drwxrwxrwx 1 root root 4.0K Aug 24 16:34 foo`，依次是: 文件权限信息 硬连接数 所属者 所属组 文件大小 创建时间  文件名
        2. `ls -i` 查看文件的iNode唯一标识
    2. `tree`
    3. `pwd` 当前所在目录
    4. `clear`清空命令行， 快捷键`ctrl L`
    5. `cd` 切换到目录
        1. `cd 目录` 切换到指定目录 
        2. `cd ~`   切换到当前用户的家目录
        3. `cd ..`  切换到上一层目录
        4. `cd .`   切换到当前目录
        5. `cd -`   切换到上一次的目录
2. 常用命令
    1. `touch 文件名` 创建指定文件
    2. `mkdir 目录名` 创建目录（文件夹）
        1. -m 设置目录权限 -p 递归创建目录
    3. `rm 文件名或目录名` 删除指定文件或者目录
        1. -r可以删除不为空的目录 -f强制删除 -i提示是否删除
    4. `rmdir 目录名` 删除空目录
    5. `cp 源文件路径 目标文件路径` 复制文件及目录， 拷贝目录必须加`-r`配置
        1. `-d` 如果不加这个-d 则拷贝软连接时会把软连接的目标文件拷贝过去，而加上后，其实只是拷贝了一个连接文件（即快捷方式）。
        2. `-r` 如果你要拷贝一个目录，必须要加-r选项，否则你是拷贝不了目录的
        3. `-i` 如果遇到一个存在的文件，会问是否覆盖。
    6. `mv 源文件 目标文件` 移动文件，也可以进行重命名  ` * 通配符`
3. 查看命令帮助
    1. `命令 --help`
    2. `man 命令` 
        1. `回车` 下一行
        2. `f` 下一屏
        3. `b` 上一屏
        4. `q` 退出
4. 查看文件内容
    1. cat  `-n` 查看文件时，把行号也显示到屏幕上 `-A` 显示所有东西出来，包括特殊字符
    2. tac 是cat的反写，同样的功能也是从尾行开始到首行 打印文件的内容到屏幕上。
    3. more 当内容特别多时，用more可以一屏一屏查看，按空格切换到下一屏，按q键退出；
        1. 下一页 f, 上一页 b, 下一行 回车， 退出q
    4. less 作用跟more一样，但比more好在可以上翻，下翻。空格键同样可以翻页，而按”j”键可以向下移动（按一下就向下移动一行），按”k”键向上移动。在使用more和less查看某个文件时，你可以按一下”/” 键，然后输入一个word回车，这样就可以查找这个word了。如果是多个该word可以按”n”键显示下一个。另外你也可以不按”/”而是按”?”后边同样跟word来搜索这个word，唯一不同的是，”/”是在当前行向下搜索，而”?”是在当前行向上搜索。
    5. head 后直接跟文件名，则显示文件的前十行。如果加 –n 选项则显示文件前n行。
    6. tail
        - tail 和head一样，后面直接跟文件名，则显示文件最后十行。如果加-n 选项则显示文件最后n行。
        - -f 动态显示文件的最后十行，如果文件是不断增加的，则用-f 选项。如：tail -f /var/log/messages
    7. `gedit 文件名` 打开记事本命令，必须在有界面的环境中使用。
    8. 管道命令(|)命令的使用
        1. 一个命令的输出可以通过管道作为另一个命令的输入，可以理解城市一个容器，存放在终端显示的内容。
        2. `ls -lh | more` 管道一般结合more命令使用，主要是分配查看终端显示内容。
5. 远程连接命令
    1. `ssh 用户名@IP`
6. vim文本编辑器
    1. 使用命令进行编辑，有三种工作模式
        1. 命令模式，编辑模式， 末行模式
        2. `:wq 保存退出  :w 保存  :q 退出  :x 保存退出  :q! 强制退出（不保存）`
    2. vim常用的命令
        1. `yy` 复制光标所在的行
        2. `p` 粘贴
        3. `dd` 删除/剪切当前行
        4. `u` 撤销
        5. `:/搜索的内容` 搜索指定内容
        6. `G` 回到最后一行
        7. `gg` 回到第一行
        8. `数字+G` 回到指定行
7. 软件的安装和卸载
    1. 离线安装和卸载
        1. `sudo dpkg -i mNetAssist-release-am64.deb` 安装离线包
        2. `sudo dpkg -r mnetassist` 卸载包 
    2. 在线安装和卸载 安装使用`apt/apt-get` .ubuntu16.04开始推荐使用apt代替apt-get
        1. 安装`sudo apt install xxx`
        2. 卸载`sudo apt remove xxx`
    3. 修改镜像源
        1. 默认是重国外的服务器下载的 可以修改为国内的镜像源下载，手动修改镜像源，配置完成之后需要执行`sudo apt-get update`
8. 重定向命令
    1. 输出重定向，把终端执行命令的结果保存到目标文件。
    2. 命令：
        1. `>` 如果文件存在会覆盖原有文件的内容，相当于文件操作中的`w`模式, `ll >  test.txt`
        2. `>>` 如果文件存在会追加写入文件末尾，相当于文件操作中的`a`模式
    3. 注意：只要在终端中能显示信息的命令都可以使用重定向，比如 tree
    4. `echo 内容` 回显命令，将echo后的参数显示在终端中，相当于`print`
        1. `echo '这是内容' >  f.txt` 把echo要展示的内容保存在文件中
9. 链接命令
    1. 软链接：类似于Windows下的快捷方式
        1. `ln -s  源文件路径(推荐绝对路径) 目标路径` s是必须的参数。
        2. 源文件刪除，軟鏈接不能使用
        3. 可以对目录创建软链接。
        4. 推荐使用绝对路径
    2. 硬链接
        1. `ln 源文件路径 目标路径`
        2. 创建一个硬链接只是给文件起一个别名，不管是源文件还是新创建的硬链接，都是文件的一个名字，删除这个名字之后，文件不一样被删除。
10. 文本搜索命令
    1. `grep -选项 '查找的内容' 文件名` `-i`忽略大小写 `-n`显示匹配的行号  `-v`显示不包含匹配的文本的所有行
    2. 查找的内容可以使用正则的方式
11. 查找文件包括目录
    1. `find 路径 -name  查找的文件名 ` 选项-name根据文件名去查找
        1. `find . name '*.txt'`
        2. `find ~ -name  '*.txt'`
    2. 通配符 `* 表示0个或多个任意字符  ？表示任意一个字符`
12. 压缩和解压缩
    1. linux默认支持的压缩格式：`.gz, .bz2, .zip`, 
        1. .gz和.bz2的压缩包需要使用tar命令来压缩和解压缩，.zip的压缩包需要使用zip命令来压缩，使用unzip命令来解压缩。
    2. tar命令和选项的使用 ``
        1. 参数
            1. `-z` 压缩成gz格式
            2. `-j` 压缩成bz2格式
            3. `-c` 创建打包文件
            4. `-x` 解包
            5. `-v` 显示详细信息
            6. `-f` 指定压缩包名，一定要放在所有选项的最后边
            7. `-C` 解压到指定目录
        3. gz格式的压缩和解压缩
            1. `tar -zcvf 压缩包名.tar.gz  要压缩的文件` 压缩   
            2. `tar -zxvf 压缩包名.tar.gz` 解压缩
            3. `tar -zxvf 压缩包名.tar.gz -C 目录` 直接解压到指定目录，目录必须存在
        4. bz2跟gz格式一样就是换了参数为-j  `tar -zcvf 压缩包名.tar.bz2  要压缩的文件`  
        5. zip命令
            1. `zip 压缩包名.zip  压缩文件`
            2. `unzip 压缩包名` 压缩到当前目录
            3. `unzip 压缩包名.zip -d 指定目录`  解压缩到指定目录

### 管理员权限和文件操作
1. 获取管理员权限的相关命令
    1. sudo命令的使用
        1. `sudo -s` 切换到root用户，获取管理员权限。
        2. `sudo` 某个命令的执行需要获取管理员权限可以在执行命令前面加上sudo
    2. `whoami` 查看当前用户是谁
    3. `exit` 突出登录用户
    4. `who` 查看所有的登录用户
    5. `passwd` 命令的使用
        1. 直接是修改用户的密码，不指定用户默认修改当前登录用户密码
        2. `sudo passwd 用户名` 直接输入新密码
    6. `which` 查看命令位置
        1. `which python`, `which ls`
    7. `shutdown -h now` 关机
    8. `reboot` 重启 

2. 文件权限
    1. 文件具有的权限
        1. `w` `r`  `x` `-`占位表示没有权限
    2. 修改权限 `chmod 权限设置 文件名` , 一般是文件的拥有者去修改权限
        1. 字母法修改 
            1. 角色：`u user所属用户， g group所属组， o other其他用户， a all 所有用户` 
            2. 操作符 `+ 添加权限， - 取消权限   = 将权限设置为...`
            3. `chmod a+r,a+w,a+x aa.txt`
        2. 数字法修改
            1. `r(4) w(2) x(1)`  706第一位所属组，第二位所属人，第三位其他人
            2. `chmod 775 文件名`， `chmod 766 文件名`

### shell命令
1. shell基础：解释执行的脚本语言，在shell中可以直接调用linux命令
    1. echo：输出命令
        1. `echo hello`, `echo -e "a\t123"`加上-e就是会执行/t换行符不加就直接输出
    2. 编写执行shell脚本文件
        1. `#!/bin/bash` test.sh 文件首行必须加次声明，告诉服务器这个文件时shell脚本
    3. 别名
        1.  临时生效的别名 `alias ls="ls -l -color=auto"`
        2. 写入环境变量 `vi ~/.bahsrc`, 编辑完成之后执行 `source ~/.bashrc` 
        3. 删除别名`unalias 别名`
    4. 命令的生效顺序
    5. 命令快捷键
        1. `ctrl c` 强制终止当前命令
        2. `ctrl l` 清屏
        3. `ctrl a` 光标移动到命令行首
        4. `ctrl e` 光标移动到命令行尾
        5. `ctrl u` 从光标所在的位置删除到行首

    6. 历史命令
        1. `history 选项 历史命令保存文件`  选项：`-c`清空历史命令 `-w`把缓存中的历史命令写入历史命令保存文件
        2. 默认保存1000条/etc/profile `HISSIZE=10000`
    7. 调用
    8. 输出重定向
    9. 管道符号
2. 变量
    1. 字符串
    ```bash
        echo 123;
        #1.变量
        your_name="jingyuan"
        echo $your_name
        echo ${your_name}
        # 变量名外面的花括号是可选的，加不加都行，加花括号是为了帮助解释器识别变量的边界，比如下面这种情况
        for skill in Ada Coffe Action Java; do
            echo "I am good at ${skill} Script"
        done

        # 2. 只读变量
        myUrl="https://www.google.com"
        readonly myUrl
        myUrl="https://www.runoob.com"
        # 3. 删除变量
        unset variable_name

        # 4.单引号和双引号的区别
        # 5.获取字符串长度
        string='abcd'
        echo ${#string}
        # 6. 截取字符串
        string="runoob is a great site"
        echo ${string:1:4} # 输出 unoo
        # 7. 查找子字符串
        echo `expr index "$string" io`
    ```

    
2. 传递参数
    1. 可以在执行shell脚本时，向脚本传递参数，脚本内获取参数的格式为：$n. n代表一个数字，1位执行脚本的第一个参数，2位执行脚本的第二个参数，依次类推
    ```sh
        #!/bin/bash
        #$0表示的执行的文件名字
        echo "Shell 传递参数实例！";
        echo "执行的文件名：$0";
        echo "第一个参数为：$1";
        echo "第二个参数为：$2";
        echo "第三个参数为：$3";
        $ chmod +x test.sh 
        $ ./test.sh 1 2 3
    ```

3. 数组
    1. 数组中可以放多个值，bash shell值支持一维数组，初始化时不需要定义数组大小，
    2. shell数组用括号类表示，元素用“空格”符号分开，语法格式：`array_name=(value1 value2 ... valuem)`
    3. 关联数组：使用`declare`命令來聲明，语法格式：`declare -A array_name`
        1. `-A`选项就是用于声明一个关联数组。
        
    ```sh
        # 1. Shell数组 数组名=(值1 值2 ... 值n)
        array_name=(value0 value1 value2 value3)
        #2. 读取数组 ${数组名[下标]}
        valuen=${array_name[n]}
        #3.获取数组的长度
        length=${#array_anme[@]};
        length=${#array_name[*]}
        #4.获取数组单个元素的长度
        length=${#array_name[n]}
        #5.关联数组
        declare -A site=(["google"]="baidu.com" ["baidu"]="baidu.com")
        site["google"]="google.com"    
        #获取数组的所有元素
        echo "数组的元素为: ${site[*]}"
    ```
4. 运算符 
    1. 算数运算符，关系运算符，布尔运算符，字符串运算符，文件测试运算符
    2. expr 是一款表达式计算工具，使用它能完成表达式的求值操作。例如，两个数相加(注意使用的是反引号 ` 而不是单引号 ')：
    ```sh
        val=`expr 2 + 2`
        val=`expr $a + $b`
        if [ $a -eq $b ]
        #布尔运算法
        [ ! false ] # 非
        [ $a -lt 20 -o $b -gt 100 ] #或
        [ $a -lt 20 -a $b -gt 100 ] #与
        #逻辑运算符
        [[ $a -lt 100 && $b -gt 100 ]]
        [[ $a -lt 100 || $b -gt 100 ]] 

    ```
5. Shell echo命令
```sh
    #1.显示普通字符串:
    echo "It is a test"
    #2.显示转义字符
    echo "\"It is a test \""
    #3.显示变量
    read 命令从标准输入中读取一行,并把输入行的每个字段的值指定给 shell 变量
        #!/bin/sh
        read name 
        echo "$name It is a test"
    #4.显示换行
    echo -e "OK! \n" # -e 开启转义
    #5.显示不换行
    echo -e "OK! \c" # -e 开启转义 \c 不换行
    #6.显示结果定向至文件
    echo "It is a test" > myfile
    #7.原样输出字符串，不进行转义或取变量(用单引号)
    echo '$name\"'
    #8.显示命令执行结果
    Thu Jul 24 10:08:46 CST 2014
```
6. Shell printf 命令
    1. printf 使用引用文本或空格分隔的参数，外面可以在 printf 中使用格式化字符串，还可以制定字符串的宽度、左右对齐方式等。默认的 printf 不会像 echo 自动添加换行符，我们可以手动添加 \n。
    2. 格式：`printf  format-string  [arguments...]`
        1. format-string 为格式控制字符串
        2. arguments 为参数列表
    ```sh
        # %s %c %d %f 都是格式替代符，％s 输出一个字符串，％d 整型输出，％c 输出一个字符，％f 输出实数，以小数形式输出。
        # %-10s 指一个宽度为 10 个字符（- 表示左对齐，没有则表示右对齐），任何字符都会被显示在 10 个字符宽的字符内，如果不足则自动以空格填充，超过也会将内容全部显示出来。
        # %-4.2f 指格式化为小数，其中 .2 指保留2位小数。
        printf "%-10s %-8s %-4s\n" 姓名 性别 体重kg  
        printf "%-10s %-8s %-4.2f\n" 郭靖 男 66.1234
        printf "%-10s %-8s %-4.2f\n" 杨过 男 48.6543
        printf "%-10s %-8s %-4.2f\n" 郭芙 女 47.9876
    ```
7. shell流程控制
    1. if elif else
    2. for 循环
    3. while 语句
    4. case esac
    5. continue break

    ```sh
        #1.if else
        a=10
        b=20
        if [ a == $b ]
        then 
            echo "a等于b"
        elif [ $a -gt $b ]
        then 
            echo "a大于b"
        elif [ $a -lt $b ]
        then
            echo "a小于b"
        else 
            echo "没有符合的条件"
        fi

        #2. if else 语句经常与 test 命令结合使用，如下所示：
        num1=$[2*3]
        num2=$[1+5]
        if test $[num1] -eq $[num2]
        then 
            echo "两个数字相等"
        
       #3. for 循环
        for loop in 1 2 3 4 5
        do 
            echo "The value is: $loop"
        done
       #4. while语句
        int=1
        while(( $int<=5 ))
        do
            echo $int
            let "int++"
        done
       #5.case ... esac
        while :
        do
            echo -n "输入 1 到 5 之间的数字:"
            read aNum
            case $aNum in
                1|2|3|4|5) echo "你输入的数字为 $aNum!"
                ;;
                *) echo "你输入的数字不是 1 到 5 之间的! 游戏结束"
                    break
                ;;
            esac
        done
    ```

8. shell函数
    1. shell中函数的定义格式如下：
        1. 可以带function fun() 定义，也可以直接fun() 定义,不带任何参数。
        2. 、参数返回，可以显示加：return 返回，如果不加，将以最后一条命令运行结果，作为返回值。 return后跟数值n(0-255)
    2. 在shell中，调用函数时可以向其传递参数。在函数内部，通过$n的形式
    ```sh
        funWithParam(){
            echo "第一个参数为 $1 !"
            echo "第二个参数为 $2 !"
            echo "第十个参数为 $10 !"
            echo "第十个参数为 ${10} !"
            echo "第十一个参数为 ${11} !"
            echo "参数总数有 $# 个!"
            echo "作为一个字符串输出所有参数 $* !"
        }
        funWithParam 1 2 3 4 5 6 7 8 9 34 73
    ```
9. Shell输入/输出重定向
10. Shell 文件包含
    1. `. filename`或者`source filename` shell也可以包含外部脚本，这样可以很方便封装一些独立的文件
    ```sh
            #!/bin/bash
        # author:菜鸟教程
        # url:www.runoob.com

        #使用 . 号来引用test1.sh 文件
        . ./test1.sh

        # 或者使用以下包含文件代码
        # source ./test1.sh

        echo "菜鸟教程官网地址：$url"
    ```

